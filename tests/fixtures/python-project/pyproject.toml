[project]
name = "fixture-python"
version = "1.0.0"

[tool.pytest.ini_options]
addopts = "-v"

[tool.ruff]
line-length = 88

[tool.mypy]
strict = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
